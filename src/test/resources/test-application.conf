# Тестовая конфигурация

http {
  host = "localhost"
  port = 8080
}

database {
  host = "localhost"
  port = 5432
  database = "switchapi"
  user = "switchapi"
  password = "switchapi"
  maxPoolSize = 10
}

jwt {
  secret = "test-secret-key-for-testing"
  expirationMs = 3600000
}
